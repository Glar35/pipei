[package]
name = "pipei_macros"
version = "0.1.0"
edition = "2024"
description = "Procedural macros for the pipei crate."
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = { version = "2", features = ["full"] }